# GENERATED FILE - do not edit directly.
# Source: docs/features/notebook-hpo/feature.source.yaml
feature_id: notebook-hpo
name: Hyperparameter optimization workflow
status: active
type: workflow
summary: Run Azure ML HPO through either a direct sweep CLI or an end-to-end AML pipeline, then review the selected best configuration before optional export into `configs/train.yaml`.
invariants:
  - invariant_id: hpo.hpo-workflow-remains-distinct-fixed-production-retraining
    statement: The HPO workflow remains distinct from the fixed production retraining workflow.
    state: active
  - invariant_id: hpo.run-hpo-pipeline-py-canonical-end-end
    statement: '`run_hpo_pipeline.py` is the canonical end-to-end HPO submission path from raw data.'
    state: active
  - invariant_id: hpo.run-hpo-py-remains-canonical-direct-rerun
    statement: '`run_hpo.py` remains the canonical direct rerun path when processed data already exists.'
    state: active
  - invariant_id: hpo.notebooks-main-ipynb-remains-live-review-surface
    statement: '`notebooks/main.ipynb` remains the live review surface.'
    state: active
  - invariant_id: hpo.exported-best-configuration-becomes-input-fixed-training
    statement: The exported best configuration becomes the input for the fixed training pipeline.
    state: active
domains:
  - hyperparameter-optimization
  - azure-ml
depends_on:
  - workspace-bootstrap
  - churn-data-preparation
capabilities:
  - capability_id: hpo.submit-end-end-azure-ml-hpo-parent-pipeline
    statement: Submit an end-to-end Azure ML HPO parent pipeline from `run_hpo_pipeline.py` that reuses validation and data prep before fan-out into sweep branches.
    state: active
  - capability_id: hpo.build-sweep-definitions-configs-hpo-yaml-another-selected
    statement: Build sweep definitions from `configs/hpo.yaml` or another selected HPO config profile.
    state: active
  - capability_id: hpo.submit-reload-sweep-jobs-azure-ml-run-hpo
    statement: Submit or reload sweep jobs in Azure ML from `run_hpo.py`, including alternate config profiles such as `configs/hpo_smoke.yaml`, while preserving the same trial-level output contract used by the HPO pipeline.
    state: active
  - capability_id: hpo.emit-hpo-summary-artifacts-hpo-summary-json-hpo
    statement: Emit HPO summary artifacts (`hpo_summary.json`, `hpo_summary.md`, `hpo_manifest/step_manifest.json`) that capture per-family results, artifact references, and the selected winner.
    state: active
  - capability_id: hpo.surface-per-family-model-output-mlflow-model-candidate
    statement: Surface per-family `*_model_output`, `*_mlflow_model`, `*_candidate_metrics`, `*_train_manifest`, and `*_hpo_manifest` outputs from the HPO parent pipeline so operators can debug one branch without opening Azure-generated controller and trial jobs first.
    state: active
  - capability_id: hpo.materialize-canonical-winner-outputs-winner-model-output-winner
    statement: Materialize canonical winner outputs (`winner_model_output`, `winner_mlflow_model`, `winner_candidate_metrics`, `winner_train_manifest`, `winner_hpo_manifest`) at the HPO parent level for downstream handoff.
    state: active
  - capability_id: hpo.materialize-winner-train-config-train-config-yaml-hpo
    statement: Materialize `winner_train_config/train_config.yaml` at the HPO parent level so fixed training can consume the selected HPO winner without manual hyperparameter copy/paste.
    state: active
  - capability_id: hpo.inspect-downloaded-remote-hpo-parent-run-through-inspect
    statement: Inspect a downloaded or remote HPO parent run through `inspect_hpo_run.py` instead of manually stitching child-job downloads.
    state: active
  - capability_id: hpo.export-selected-hpo-winner-fixed-train-config-yaml
    statement: Export the selected HPO winner into a fixed train-config YAML through `export_hpo_winner_config.py` so the standard train pipeline can retrain the chosen family without manual parameter copy/paste.
    state: active
  - capability_id: hpo.treat-inspect-hpo-run-py-export-hpo-winner
    statement: Treat `inspect_hpo_run.py` and `export_hpo_winner_config.py` as operator-facing wrapper commands whose implementation ownership lives under `tools/hpo/`.
    state: active
  - capability_id: hpo.treat-configs-hpo-smoke-yaml-wiring-artifact-profile
    statement: Treat `configs/hpo_smoke.yaml` as a wiring/artifact profile and the default `configs/hpo.yaml` path as the heavier-weight ranking surface.
    state: active
  - capability_id: hpo.reuse-shared-src-azureml-client-input-adapters-instead
    statement: Reuse the shared `src/azureml/` client and input adapters instead of wiring Azure ML SDK calls directly inside every orchestration path.
    state: active
  - capability_id: hpo.analyze-completed-sweep-results-update-configs-train-yaml
    statement: Analyze completed sweep results and update `configs/train.yaml` with the chosen model configuration from the notebook review surface.
    state: active
  - capability_id: hpo.provide-review-surface-periodic-re-optimization-becoming-sole
    statement: Provide a review surface for periodic re-optimization without becoming the sole operational control plane.
    state: active
  - capability_id: hpo.keep-hpo-utils-py-supporting-shared-utility-code
    statement: Keep `hpo_utils.py` as supporting shared utility code rather than treating it as an operator entrypoint.
    state: active
refs:
  code:
    - hpo_utils.py
    - run_hpo.py
    - run_hpo_pipeline.py
    - src/azureml/__init__.py
    - src/azureml/client.py
    - src/azureml/inputs.py
    - src/collect_hpo_results.py
    - src/materialize_hpo_winner.py
    - src/run_sweep_trial.py
    - tools/hpo/export_hpo_winner_config.py
    - tools/hpo/inspect_hpo_run.py
    - tools/retraining/run_retraining_hpo_smoke.py
    - tools/retraining/run_retraining_hpo_to_fixed_train.py
  tests:
    - tests/test_azureml_adapters.py
    - tests/test_collect_hpo_results.py
    - tests/test_export_hpo_winner_config.py
    - tests/test_inspect_hpo_run.py
    - tests/test_materialize_hpo_winner.py
    - tests/test_run_hpo_config.py
    - tests/test_run_hpo_pipeline.py
    - tests/test_run_retraining_hpo_smoke.py
    - tests/test_run_retraining_hpo_to_fixed_train.py
  docs:
    - docs/pipeline_guide.md
    - docs/portfolio_architecture_spec.md
  configs:
    - configs/data.yaml
    - configs/data_smoke.yaml
    - configs/data_smoke_eval.yaml
    - configs/hpo.yaml
    - configs/hpo_smoke.yaml
    - configs/train_ranking_hpo_winner.yaml
    - configs/train_smoke_hpo_winner_rf.yaml
  components:
    - aml/components/collect_hpo_results.yaml
    - aml/components/data_prep.yaml
    - aml/components/hpo_trial.yaml
    - aml/components/materialize_hpo_winner.yaml
    - aml/components/validate_data.yaml
  specs:
  plans:
revision: 13
latest_change_id: 2026-04-18-feature-freshness-metadata
last_updated_at: '2026-04-19T00:00:40+02:00'
