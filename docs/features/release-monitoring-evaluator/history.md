# History

## 2026-04-14

- Created the initial feature contract for the first bounded monitor-stage evaluator.
- Scoped the evaluator to release evidence first and optional sampled inference evidence second.
- Kept the feature explicitly separate from unsupported Azure collector-backed monitoring assumptions.
- Implemented `run_monitor.py` and `src/monitoring/evaluate_release.py` as the first repo-supported monitoring workflow.
- Added bounded monitor artifacts: `monitor_summary.json`, `monitor_report.md`, and `monitor_manifest/step_manifest.json`.
- Verified the healthy `release_evidence_only` path end to end against saved release artifacts while preserving proof-gated `capture_backed` behavior.
- Extended the evaluator to consume retrieved production capture directories, not just single files, and added bounded class-balance checks before `capture_backed` can be considered healthy.
- Added `--capture-path` to `run_monitor.py` and a thin `aml/components/monitor.yaml` wrapper so scheduled runs can reuse the same evaluator contract instead of forking monitoring logic.
- Added `run_inference_capture.py` and `src/inference/client_capture.py` as the repo-owned caller-side capture path, so capture evidence can be written by the invoking wrapper or a future gateway instead of relying on Azure managed-online scoring internals.
- Proved the caller-side path against `churn-endpoint/churn-deployment` by writing three redacted JSONL records to Blob, downloading those exact records, and reaching `monitor_status = capture_backed` with `capture_record_count = 3`.
- Added `run_release_monitor_smoke.py` as a thin composition layer that can reuse an existing `release_record.json` or run `run_release.py`, then invoke caller capture, download the exact records referenced by the capture manifests, and feed them into `run_monitor.py`.
- Proved the new wrapper in reuse mode against the live endpoint with `sample-data.json`, `payload_b.json`, and `payload_c.json`; the run finished with `capture_backed`, downloaded three exact manifest-referenced records, and preserved the existing `release_record.json` as the monitoring handoff source.
- Patched the monitoring artifact contract so `release_record.json` exposes `deployment_capture` as deployment-owned truth, `monitor_summary.json` exposes `capture_evidence_source` and `caller_capture`, and `release_monitor_summary.json` reconciles deployment capture, caller capture, and final handoff state without implying they are the same thing.
- Verified that the monitor evaluator remains truthful after a bounded scorer failure probe: rechecking the proven `modest_caravan_mc3tt6sltd` release record after an intentional Azure scorer `ValueError` still produced `monitor_status = limited_but_healthy`, `evidence_level = release_evidence_only`, and `runtime_contract = repo_owned_scoring_proven`.
- Added `run_monitor_handoff.py` as the monitoring-first repeatable handoff surface so operators can start from a saved `release_record.json`, gather fresh caller-side evidence, and produce a stable handoff bundle without redeploying.
- Proved the new wrapper against the saved `modest_caravan_mc3tt6sltd` release record; because capture sink credentials were not configured in that run, the wrapper stayed truthful with `caller_capture.status = not_run`, `monitor_status = limited_but_healthy`, and `handoff.status = release_evidence_only_ready`.
- Hardened exact Blob-backed caller capture retrieval with bounded retry plus a corrected `azureblob://<account>/<container>/<blob>` reader contract so the downloader now resolves the same blob path the writer uploaded.
- Re-ran the same repeatable handoff proof against `modest_caravan_mc3tt6sltd` with Blob capture configured and reached full `capture_backed` status: `caller_capture.status = retrieved`, `capture_record_count = 3`, `monitor_status = capture_backed`, and `handoff.status = capture_backed_monitoring_ready`.
- Added the first bounded retraining-policy layer to the monitor evaluator: `monitor_summary.json` now embeds `retraining_policy`, `run_monitor.py` writes `retraining_decision.json`, and the current policy distinguishes `no_retraining_signal`, `retraining_candidate`, and `investigate_before_retraining` without auto-starting train, HPO, promotion, or release.
- Added `run_retraining_candidate.py` as the next bridge after a positive monitor retraining decision; it freezes explicit `current_data` / `reference_data` identities, writes `retraining_candidate_manifest.json` plus `validation_handoff.json`, and can optionally reuse `validate_data.py` without auto-starting downstream train or HPO work.
- Added `run_retraining_fixed_train_smoke.py` as the next smoke-only bridge after a passed candidate validation; it turns the frozen candidate plus validation result into an exact fixed-train invocation contract and optional `run_pipeline.py` submission without collapsing the monitor-driven lineage into ad hoc operator glue.
- Added `run_retraining_path_selection.py` as the next thin bridge after candidate validation; it turns a passed validation plus retraining recommendation into one explicit selected-path artifact, can invoke the fixed-train smoke bridge, and otherwise emits a prepared HPO handoff without pretending the HPO execution bridge already exists.
- Enriched the bounded monitor retraining policy with explicit path-recommendation evidence: `retraining_decision.json` and `monitor_summary.json` now surface `path_recommendation`, `path_recommendation_reason_codes`, `drift_severity`, `signal_persistence`, and `policy_confidence`, and `monitor_report.md` plus `monitor_manifest/step_manifest.json` keep those fields operator-visible without auto-starting downstream train or HPO work.
- Added `run_retraining_loop.py` as the thin phase-1 coordinator over the existing candidate, validation, path-selection, fixed-train, and HPO bridges; at introduction time it supported explicit stop modes, wrote `retraining_loop_summary.json`, `retraining_loop_report.md`, and `retraining_loop_manifest/step_manifest.json`, and still stopped before HPO winner export, promotion, or release.
- Extended `run_retraining_loop.py` with an opt-in release continuation surface: after downstream promotion-facing evidence is proven promotable, the loop can now delegate to `run_release.py` and optionally `run_monitor_handoff.py` while keeping child release and handoff artifacts as the authoritative truth.
- Completed the first bounded end-to-end loop proof on `modest_sponge_hxk5b23vty`: the loop started from a monitor retraining decision, passed candidate validation, selected `fixed_train`, continued through release, reran caller-side capture plus exact Blob retrieval, and finished with `monitor_handoff_status = capture_backed_monitoring_ready`.
- Added negative-proof coverage for the newest loop/release/monitor boundaries and patched the remaining truthfulness gap: when post-release monitor handoff fails, `run_retraining_loop.py` now stops with `final_stage = monitor_handoff` and loop `status = failed` while preserving the succeeded child `release_record.json` as release truth.
- Completed the `model_sweep` branch proof at the child-surface level for post-release monitoring: after HPO continuation produced promotable child `heroic_boniato_jk85w0dvkg`, the deployed release reached caller-side capture retrieval with three exact Blob-backed records and ended at `handoff.status = capture_backed_monitoring_ready`.
- Closed the top-level `model_sweep` loop proof deterministically through the new resume seam: the resumed loop re-used promotable continuation evidence, succeeded through release and monitor handoff, and truthfully recorded a `release_evidence_only_ready` handoff when deployment capture was disabled in that deterministic closure run.
